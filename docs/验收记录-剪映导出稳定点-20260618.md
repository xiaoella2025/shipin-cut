# 剪映导出稳定点验收记录 - 2026-06-18

## 稳定 Commit

`5333b93 fix export timing normalization`

## 验收结论

- 成品视频 1 导出剪映草稿成功。
- 成品视频 2 导出剪映草稿成功。
- 成品视频 3 导出剪映草稿成功。
- 方案 2 不再出现 `50.84s -> 59.86s` 的时长漂移。
- 方案 3 导出正常。
- 剪映草稿包含视频轨和字幕轨。
- 字幕轨可编辑。
- 最后一段有画面、原声、字幕。
- 失败时不生成坏草稿的机制继续保留。

## 根因记录

- V2 源视频为非常规 `51fps / time_base=1/13056`。
- 单个切片时长正常，但 concat 后视频轨、音频轨、容器时长分离。
- 方案 2/3 因混入 V2 后 concat 时间轴漂移，导致 clean mp4 从 expected 约 `50.84s` 变成约 `59.86s`。
- 该问题不属于字幕识别、剪映草稿生成、导入视频或最后一段本身的问题。

## 修复摘要

- 切片阶段统一 `fps=30`。
- 统一 CFR。
- 统一 `setpts / asetpts`。
- 统一 timebase。
- 统一画布尺寸。
- 统一音频采样。
- concat filter 对每路输入重新规范化。
- 移除导出链路中会保留旧 timestamp 的 `-c:v copy` 分支。
- 保留 duration mismatch hard-fail。
- debug report 记录 fps、time_base、format/video/audio duration 信息。

## 后续禁止回退项

- 不要恢复 `-c:v copy` 到导出链路。
- 不要取消 duration mismatch hard-fail。
- 不要取消 fps/timebase/PTS 统一。
- 不要破坏无额外配音时保留原视频原声。
- 不要破坏剪映草稿干净视频 + 可编辑字幕轨。
- 不要恢复假字幕。

## 后续回归必测

- 普通 MP4 导出。
- 剪映草稿导出。
- 方案切换后字幕不串。
- 字幕手动修改后导出是否生效。
- 最后一段画面/原声/字幕。
- 多素材混入非常规 fps 视频时，导出时长仍接近 expectedDuration。
