# 剪映字幕体系稳定点验收记录 - 2026-06-18

## 稳定 Commit

`429368a fix Jianying subtitle material export`

## 验收结论

- 方案 1 导出剪映草稿后，右侧出现「字幕」页。
- 方案 2 导出剪映草稿后，右侧出现「字幕」页。
- 方案 3 导出剪映草稿后，右侧出现「字幕」页。
- 「字幕」页中有完整字幕列表。
- 字幕可以点选编辑和调整。
- 时间线字幕条正常。
- 画面字幕正常显示。
- 视频轨正常。
- 原声正常。
- 时长漂移问题没有回退。

## 根因记录

- 旧逻辑使用 `pyJianYingDraft.TextSegment` 写入 `TrackType.text`。
- 旧 `materials.texts[].type = "text"`。
- 剪映因此把字幕识别为普通文本条，只显示「文本」页。
- 用户无法使用剪映右侧「字幕」页、字幕列表、统一字幕管理功能。

## 修复摘要

- 保持 track 类型与剪映样本兼容。
- 将 `materials.texts[].type` 从普通 `text` 升级为 `subtitle`。
- 补充 subtitle material 所需字段，例如 `sub_type`、`recognize_type`、`caption_template_info`、`words`、`subtitle_keywords` 等。
- 保留普通 text clip fallback：`--subtitle-mode text`。
- 新增 `tests/jianyingSubtitleMaterial.test.mjs`。
- 方案 1/2/3 均生成 subtitle material 草稿。

## 后续禁止回退项

- 不要把剪映字幕重新降级成普通 text material。
- 不要删除 subtitle material 字段。
- 不要删除字幕列表能力。
- 不要破坏剪映右侧「字幕」页。
- 不要破坏已有的 1/2/3 剪映草稿导出。
- 不要破坏 `5333b93` 的 fps/timebase/concat 时长修复。
- 不要恢复假字幕。

## 后续回归必测

- 导出剪映草稿后是否出现「字幕」页。
- 「字幕」页是否有完整字幕列表。
- 字幕是否可编辑。
- 方案切换是否串字幕。
- 字幕时间是否正确。
- 视频时长是否接近网页 expectedDuration。
- 最后一段是否有画面、原声、字幕。
